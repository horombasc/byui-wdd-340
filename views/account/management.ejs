<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title><%= title %></title>
  <link rel="stylesheet" href="/css/styles.css" />
</head>
<body>
  <main>
    <!-- Display flash messages -->
    <%- messages() %>

    <!-- Greeting -->
    <h2>Welcome <%= accountData.account_firstname %></h2>

    <!-- Update Account Link (Always visible) -->
    <p><a href="/account/update/<%= accountData.account_id %>" title="Update your account information">Update Account Information</a></p>

    <!-- Inventory Management (Employees/Admins only) -->
    <% if (accountData.account_type === 'Employee' || accountData.account_type === 'Admin') { %>
      <h3>Inventory Management</h3>
      <p>
        Access the tools to manage vehicle inventory: 
        <a href="/inv" title="Go to Inventory Management Tools">Manage Inventory</a>
      </p>
    <% } %>

  </main>
</body>
</html>




